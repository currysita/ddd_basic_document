# 用語集：第11章「ファクトリ」

> 本章（実践ドメイン駆動設計 第11章）で登場する用語の定義集です。
> 五十音順。英数字で始まる用語は末尾にまとめています。

---

## あ行

### アダプター（Adapter）

異なるインターフェースや異なるコンテキスト間のオブジェクト変換を担うデザインパターン。複数の境界づけられたコンテキストにまたがる変換型ファクトリによく使用される。

---

### アトミックな生成（Atomic Creation）

ファクトリメソッドは生成操作をひとまとまり（アトミック）に行い、中途半端な状態のオブジェクトが外部に漏れないようにする設計指針。Evans 氏が優れたファクトリの条件として挙げている。

---

### アブストラクトファクトリ（Abstract Factory）

複数の関連オブジェクト群を生成するインターフェースを提供するデザインパターン。ファクトリ実装の選択肢の一つ。

---

## か行

### コラボレーターサービス（Collaborator Service）

認証・アクセスコンテキストのユーザー情報をコラボレーションコンテキストのロールオブジェクト（投稿者・作成者・モデレーター・所有者・参加者）に変換するドメインサービス。複数の境界づけられたコンテキスト間の変換型ファクトリとして機能する。

---

## は行

### ビルダー（Builder）

複雑なオブジェクトの生成手順を段階的に組み立てるデザインパターン。ファクトリ実装の選択肢の一つ。

---

### ファクトリ（Factory）

複雑なオブジェクトや集約のインスタンスを生成する責務を担うオブジェクト・メソッド。ドメインモデルにおいて何の責務も負っていないかもしれないが、それでもドメイン設計の一部である。生成のロジックをクライアントから隠蔽し、ユビキタス言語に沿った生成処理をシンプルに表現する。

---

### ファクトリメソッド（Factory Method）

集約ルートや集約に定義された、別のオブジェクトを生成して返すメソッド。実践ドメイン駆動設計では「集約の振る舞い（メソッド）」としてファクトリを実装することが主に紹介されている。例：`Forum.StartDiscussionFor()` → `Discussion` を生成して返す。

---

### 不変条件（Invariant）

集約やオブジェクトが常に満たさなければならない整合性ルール。ファクトリは生成時に不変条件を検証する責務を持つ。例：`Forum.AssertOpen()` でフォーラムがオープン状態であることを確認する。

---

## や行

### ユビキタス言語（Ubiquitous Language）

ドメインの専門家と開発者が共有する言語。ファクトリのメソッド名・戻り値の型はユビキタス言語を忠実に表現するべきである。例：`StartDiscussionFor`（フォーラムでディスカッションを開始する）。

---

## 英数字

### EventSourcedRootEntity

SaaSOvation における集約ルートの基底クラス。イベントソーシングをサポートする。`Forum` クラスはこれを継承し、`StartDiscussionFor` ファクトリメソッドを持つ。

---

### ICollaboratorService

コラボレーションコンテキストにおける変換用ドメインサービスのインターフェース。`GetAuthorFrom`・`GetCreatorFrom`・`GetModeratorFrom`・`GetOwnerFrom`・`GetParticipantFrom` の各ファクトリメソッドを定義する。

---
